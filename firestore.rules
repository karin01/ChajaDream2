rules_version = '2';
// 개발 단계: 2026-05-31 이후에는 쓰기 차단 — 콘솔에서 규칙 갱신하거나 인증 기반 규칙으로 교체할 것
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 6, 1);
    }
  }
}
